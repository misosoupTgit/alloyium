<div align="center">

# Alloyium

<img src="alloyium.png" alt="Alloyium" width="220" />

</div>

# 技術ポストモーテム：OpenGL 4.3 Compute Shader による Mesh Shader エミュレーションの限界
## ── Alloyium (Nvidium 互換・クロスベンダー GPU 駆動地形描画 MOD) 開発記録

対象: Minecraft 1.20.1 / Forge / Embeddium 依存
本書の位置付け: **なぜ「アルゴリズムは移植できた」のに「性能は移植できなかった」のか**を、GPU アーキテクチャのレベルまで踏み込んで定量的に再構成した記録。結論・是正方針・検証手順は本書単体で完結する。

---

## 目次

1. エグゼクティブサマリー
2. 前提知識：GPU のメモリ階層と Mesh Shader が「バイパスしているもの」
3. 実装9割時点で何が起きていたか（時系列）
4. ボトルネック分析 I — VRAM 往復コストの定量化
5. ボトルネック分析 II — Command Processor（フロントエンド）のシリアライズ
6. ボトルネック分析 III — Embeddium の事前最適化とのバッティング
7. なぜ「正しく動くのに遅い」状態が9割地点まで見つからなかったか
8. 是正後アーキテクチャ（Compute B'）の詳細設計
9. 測定方法論：何を、どのツールで、どう測るか
10. それでも埋まらない差：Mesh Shader の物理的優位性の内訳
11. 決定事項と今後のロードマップ
12. 付録：GLSL 疑似コード集
13. 参考文献

---

## 1. エグゼクティブサマリー

Alloyium は「Nvidium（`GL_NV_mesh_shader` 依存）がやっていることの大半はアルゴリズムであり、ハードウェア結合部分を除けば OpenGL 4.3 Compute Shader + SSBO + Indirect Draw で再現できる」という仮説から出発した。

この仮説は**半分正しく、半分間違っていた**。

- **正しかった部分**: Frustum Culling、Normal Cone Culling、Stream Compaction、Indirect Dispatch/Draw によるコマンド生成は、アルゴリズムとして 1:1 で Compute に移植可能だった。実際、カリング精度・コンパクション結果の正当性は検証済みで、これ自体にバグはない。
- **間違っていた部分**: 「Mesh Shader が頂点を変換して出力する」という工程まで Compute で模倣しようとした。これは Mesh Shader ハードウェアが**オンチップの Shader Export 経路**を使ってラスタライザに直結しているからこそ低コストなのであって、Compute Shader が SSBO 経由でこれを行うと、Mesh Shader には存在しない「VRAM への書き込み → 別ステージでの VRAM からの再読み込み」という往復コストを新規に背負うことになる。

結果として、実装が9割まで進んだ時点で **Vanilla + Embeddium 単体よりも FPS が低下する**という、当初の目的（高速化）と正反対の事態が発生した。原因はカリング側ではなく、可視と判定された後の「頂点データの転送経路」に閉じていた。

是正の骨子は単純である：

> **カリングとコマンド生成だけを Compute でやり、頂点属性そのものは一切動かさない。** 最終描画では常に Embeddium が既に確保している静的 VBO/IBO を `baseVertex` / `firstIndex` 経由でそのまま参照する。

この是正（Compute B → Compute B' への役割格下げ）により、往復コストそのものが構造的に消滅する設計に変わった。ただし、この是正は「Mesh Shader 相当の機能を全部持つ」という初期の野心を後退させるものでもあり、最終的に Alloyium が到達できる性能上限は Nvidium より低い場所にあることを、設計として明示的に受け入れている。

---

## 2. 前提知識：GPU のメモリ階層と Mesh Shader が「バイパスしているもの」

この節が本書の核心である。「VRAM 往復ペナルティ」という言葉だけでは不十分であり、**なぜ**問題なのかを定量的に見ないと、次に似た罠を踏む可能性が高い。

### 2.1 GPU のメモリ階層（NVIDIA Turing/Ampere を例に）

GPU のメモリは、CPU 同様に「速いが小さい」層と「遅いが大きい」層のピラミッド構造を持つ。おおよその特性（アーキテクチャにより変動するので桁感で捉える）：

| 階層 | 容量目安（SM/CU あたり） | レイテンシ目安 | 帯域目安 |
|---|---|---|---|
| レジスタ | 数百 KB | 数サイクル | 実効的に無限大に近い（並列アクセス） |
| Shared Memory / LDS | 64–128 KB | 20–30 サイクル | 数十 TB/s 級（オンチップ） |
| L1 / テクスチャキャッシュ | 128 KB 前後 | 数十〜100 サイクル | 数 TB/s 級 |
| L2 キャッシュ | 数 MB（チップ全体で共有） | 200 サイクル前後 | 1–2 TB/s 級 |
| VRAM (GDDR6/6X) | 数 GB〜数十 GB | 400–600 サイクル以上 | 300〜1000 GB/s 級（GPU グレードによる） |

重要なのは、**Shared Memory / レジスタと VRAM の間には 2〜3 桁のレイテンシ差、1〜2 桁の実効帯域差がある**という点である。

### 2.2 Mesh Shader の Shader Export 経路

Turing 以降の Mesh Shader パイプラインでは、Task/Mesh Shader が生成した頂点・プリミティブデータは、専用の **Shader Export（`exp` 命令系）** という固定機能ハードウェア経路を通って、SM 内のオンチップバッファから直接プリミティブアセンブラ／ラスタライザへストリーミングされる。

概念図（簡略化）：

```
[Mesh Shader (SM 内)]
   shared memory / register に頂点・index を構築
        │  ← ここがオンチップ。VRAM を経由しない
        ▼
[Shader Export ハードウェアユニット]
        │  ← 専用インターコネクト。汎用メモリバスと分離
        ▼
[Primitive Assembler] → [Rasterizer] → [ROP]
```

この経路の要点：

1. **VRAM への書き込みが発生しない**（少なくとも頂点/プリミティブデータの受け渡しにおいては）。
2. **専用ハードウェアパスなので、L2 キャッシュや VRAM コントローラの帯域を消費しない**。テクスチャフェッチやフレームバッファ書き込みと帯域を奪い合わない。
3. `SetMeshOutputCounts()` によって、Export バッファの必要量がハードウェア側で事前に確保・管理される。これにより Occupancy（同時実行 SM 数）が自動的に調整され、バッファ溢れが起きない設計になっている。

### 2.3 Compute Shader + SSBO で同じことをするとどうなるか

OpenGL 4.3 Compute Shader には Shader Export に相当する機構が存在しない。Compute Shader の出力は必ず **SSBO（Shader Storage Buffer Object）= VRAM 上のバッファ** に書き込むしかない。そして次のステージ（頂点シェーダ）は、そのデータを**もう一度 VRAM から読み込む**必要がある。

```
[Compute Shader (旧 Compute B)]
   頂点変換（TransformVertex）を実行
        │
        ▼
   SSBO へ書き込み（VRAM Write）★1回目のVRAM往復
        │
        ▼
[頂点シェーダ (VS)]
   同じ SSBO を読み込み（VRAM Read）★2回目のVRAM往復
        │
        ▼
[Rasterizer] → [ROP]
```

Mesh Shader 版には存在しない「VRAM Write」と「VRAM Read」の**2回分のメモリトラフィックが丸ごと追加**される。しかも、この追加コストは「カリングで頂点数を減らした後」の可視データに対してであっても、ゼロにはならない構造的なコストである。

### 2.4 定量イメージ（オーダー感の把握用。実測ではなく概算）

以下は「桁がどれくらいになるか」を掴むための概算であり、実測値ではない。実際の値は §9 の手順でプロファイラにより測定する必要がある。Compute B' 移行の完了判定も、この実測（SSBO Write バイト数の大幅減少と、フレーム GPU time が Vanilla+Embeddium を下回ること）に置く。

**前提（仮定値）**:
- Embeddium の圧縮頂点フォーマットはおおよそ 1 頂点あたり 16〜24 バイト程度（位置・法線・UV・ライト値をパックした構成が一般的）。ここでは 20 バイト/頂点と仮定する。
- 描画距離 12 チャンク（Minecraft 標準的な中距離設定）でカリング後に画面に映る頂点数は、シーン密度により大きく変動するが、目安として数百万頂点/フレームのオーダーになりうる。ここでは 4,000,000 頂点/フレームと仮定する。
- GPU の VRAM 実効帯域を 300 GB/s と仮定（ミドルレンジクラス）。

**計算**:

```
往復データ量 = 頂点数 × バイト/頂点 × 2（Write + Read）
            = 4,000,000 × 20 byte × 2
            = 160,000,000 byte
            ≈ 152.6 MB / フレーム

理論転送時間 = 152.6 MB / 300 GB/s
            ≈ 0.51 ms / フレーム
```

60 FPS のフレーム予算は 16.6 ms なので、**帯域だけで見ると 0.51 ms は 3% 程度**であり、一見すると致命的ではないように見える。ここが罠である。実際に FPS 低下を引き起こしていた要因は、単純な帯域占有ではなく、以下の**複合効果**だと考えられる：

1. **アクセスパターンの非コアレス性**: Mesh Shader の Export はスレッドインデックス順の書き込みを前提にハードウェアが最適化されている。Compute → SSBO → VS という 2 パス構成では、書き込み側（Compute のワークグループ単位）と読み込み側（VS のプリミティブ単位）でアクセス順序が一致しない場合があり、キャッシュラインの再利用効率が落ちる。理論帯域に対する実効帯域の乖離（実効効率 30〜60% 程度に落ちることも珍しくない）が発生しうる。
2. **`glMemoryBarrier()` によるパイプラインストール**: Compute の書き込みが完了したことを VS の読み込み前に保証するため、`GL_SHADER_STORAGE_BARRIER_BIT` を伴う `glMemoryBarrier()` が必要になる。これは **GPU 全体の書き込みキューをフラッシュしてから次のステージに進む**同期点であり、後段の作業がキャッシュに乗っていても待たされる。バリアのコストは転送量に比例しない固定オーバーヘッドを含む。
3. **他パスとの帯域競合**: 同じフレーム内でテクスチャフェッチ（ブロックアトラス参照）やフレームバッファ書き込みが VRAM 帯域を使っている。頂点往復トラフィックはこれらと**同じ物理帯域を取り合う**ため、単独では 3% でも、ボトルネック帯域が既に飽和気味の GPU bound な場面では相対的な悪影響が拡大する。
4. **軽量 VS コストとの比較で「割に合わない」**: Minecraft の地形 VS は行列変換とテクスチャ座標のパススルー程度で、そもそも計算コストが極めて軽い。カリングで減らせる頂点変換コストの絶対量が小さいため、「往復コストを払ってまで頂点変換をオフロードする」投資対効果が、CAD 密メッシュのケースに比べて著しく低い。

**結論**: 単一の指標（総帯域）だけでは説明しきれないが、**同期コスト・アクセスパターン・軽量 VS という3つの要因が重なることで、カリングによる削減効果を相殺、あるいは上回る**。この見立ては GPU アーキテクチャ的にも妥当な仮説である。

---

## 3. 実装9割時点で何が起きていたか（時系列）

| フェーズ | 状態 |
|---|---|
| M1–M3 | GL 能力検知、Embeddium 橋渡し、Frustum Culling + Indirect solid 描画まで完了。Vanilla+Embeddium 比で改善が見え始める |
| M4 | Stream Compaction 高度化（Atomic Counter → shared Prefix Sum）。CPU 側の draw call 発行がほぼゼロになる |
| （旧）M5 相当 | **Compute B（頂点変換＋SSBO書き出し）を実装**。カリングされた可視頂点のみを対象にしているため「これで速くなるはず」という想定だった |
| 実装9割地点 | ベンチマークで **Vanilla+Embeddium 単体を下回る FPS** を計測。コミット参照: `a42fb8f` 系 |
| 原因調査 | RenderDoc/Nsight でパス別 GPU time を分解 → Cull パス（Compute A）は想定通り高速。**Compute B のディスパッチ〜VS 実行間で予想外のストール**を確認 |
| 是正判断 | Compute B の役割を「頂点変換・出力」から「Indirect 引数／圧縮 index 生成のみ」へ格下げ（Compute B'）。以降の設計・実装の正本とする |

この時系列で重要なのは、**カリングロジック自体は最初から正しく機能していた**という点である。バグではなく、**「模倣する対象を選び間違えた」という設計判断の誤り**だったことが、この後の是正内容を決定づけている。

---

## 4. ボトルネック分析 I — VRAM 往復コストの定量化（詳細版）

### 4.1 なぜ「カリング後だから大丈夫」という直感が外れたか

Compute A（カリング）の後段で Compute B が処理するのは、確かに「可視と判定された頂点」だけである。したがって直感的には「カリングで頂点数を絞ったのだから、往復コストも絞られているはず」と考えたくなる。

しかし、これは「カリングで削減できる量」と「往復コストの絶対量」を混同した推論である。カリングは**描画すべき頂点数そのものを減らす**最適化であり、Mesh Shader ならそのまま「オンチップ転送量の削減」に直結する。しかし Compute B 方式では、**削減後の頂点数に対してもなお VRAM Write + Read という固定的なオーバーヘッド構造が残る**。カリング効果と往復コストは加算的ではなく、**カリングは往復コストの「量」を減らすだけで、「構造」自体は消えない**。

言い換えると：

```
Mesh Shader:      総コスト = 頂点変換コスト（オンチップ）
Compute B(旧):    総コスト = 頂点変換コスト + VRAM Write + VRAM Read + 同期コスト
```

カリングは両者の「頂点変換コスト」を等しく削減するが、Compute B(旧) 側にだけ存在する後半3項は削減されない（正確には頂点数に比例して減りはするが、係数がゼロにはならない）。この非対称性が、カリング精度が完璧であっても FPS が改善しない、あるいは悪化する現象の本質である。

### 4.2 Compute B' 方式ではこの構造が消える理由

是正後の Compute B' は、**頂点属性を一切生成・書き出ししない**。生成するのは `DrawElementsIndirectCommand`（`count`, `instanceCount`, `firstIndex`, `baseVertex`, `baseInstance`）という、頂点1個あたり数バイトではなく**描画コマンド1個あたり 20 バイト程度の軽量な構造体**だけである。

```c
// OpenGL の DrawElementsIndirectCommand 構造（4.3 コア仕様）
typedef struct {
    uint  count;         // このコマンドが描画するインデックス数
    uint  instanceCount; // インスタンス数（通常 1）
    uint  firstIndex;    // Embeddium 静的 IBO 内のオフセット
    int   baseVertex;    // Embeddium 静的 VBO 内のオフセット
    uint  baseInstance;
} DrawElementsIndirectCommand; // 20 bytes
```

可視 meshlet（またはセクション）1個につきこの 20 バイトを書くだけであり、**頂点数に比例しない**。仮に 1 meshlet = 64 頂点として、10,000 個の可視 meshlet があったとしても：

```
Compute B(旧) 相当の往復:  10,000 × 64 × 20 byte × 2 ≈ 25.6 MB
Compute B' の書き込み:     10,000 × 20 byte         ≈ 195 KB
```

**約130倍のデータ量差**になる。しかも Compute B' の出力は VS 側で再読み込みされるのではなく、`glMultiDrawElementsIndirect` の**コマンドプロセッサ**が読み込むものであり、頂点シェーダの実行そのものは Embeddium の静的 VBO を通常の頂点属性フェッチ経路（VRAM 上に固定配置され、ドライバによりキャッシュ最適化済み）から直接読む。これは Vanilla+Embeddium の通常描画と同じ経路であり、Alloyium 固有の追加コストがゼロになる。

---

## 5. ボトルネック分析 II — Command Processor（フロントエンド）のシリアライズ

Compute B' への移行で頂点往復コストは解消したが、これは「Compute A/B' が生成する Indirect コマンドの**数**そのもの」という別のボトルネックを相対的に顕在化させた。

### 5.1 `glMultiDrawElementsIndirect` の内部動作

`glMultiDrawElementsIndirect(mode, type, indirect, drawcount, stride)` は、GPU 側の GL_DRAW_INDIRECT_BUFFER に格納された `drawcount` 個のコマンド配列を**逐次的に**発行する API である。OpenGL 4.3 コアの範囲では：

- `drawcount` 自体は**CPU 側から渡す固定値**であり、GPU 側でカリング後に決定した「実際に必要なコマンド数」を動的に反映する術がない（`GL_ARB_indirect_parameters` の `glMultiDrawArraysIndirectCountARB` を使えば可能だが、これは 4.3 コアの範囲外の拡張であり、対応 GPU に依存する）。
- 結果として、Alloyium は「最大想定数のコマンドバッファを毎回発行し、不要なコマンドは `count=0` にして実質 no-op にする」という**間引き漏れ吸収策**を取らざるを得ない場面がある。`count=0` のコマンドも Command Processor はパースするため、無駄なパース処理が残る。
- GPU の Command Processor（AMD で言う ACE、NVIDIA の GigaThread Engine 相当のフロントエンド）は、原則として**コマンドストリームを順番に解釈してから**実行ユニットにディスパッチを投げる、固定機能寄りの回路である。これは汎用の SIMT 実行ユニットのように並列にスケールしない。

### 5.2 meshlet 粒度の細分化が逆効果になるメカニズム

Alloyium のカリング粒度を meshlet 単位（V=64, T=84 など）まで細かくすると、1フレームあたりの Indirect コマンド数は「セクション単位カリング」の場合と比べて 1〜2 桁増加しうる。例えば 1 セクション（16×16×16 ブロック）が平均 20〜30 meshlet に分割されるとすれば：

```
セクション単位カリング:  可視セクション数 ≈ 数千個 → コマンド数 数千
meshlet 単位カリング:    可視セクション数 × 20〜30 → コマンド数 数万〜十万
```

コマンド数が1桁増えると、Command Processor のパース時間もほぼ比例して増加する。加えて、`glMemoryBarrier(GL_COMMAND_BARRIER_BIT)` のような同期点を挟む場合、**このパースはドライバによってはシングルスレッド的に処理される**ため、並列実行ユニット（SM/CU）がアイドルのままフロントエンドの処理待ちになる時間帯が生じる。

### 5.3 モダン API との比較

| 機能 | OpenGL 4.3 コア | Vulkan / DirectX12 相当 |
|---|---|---|
| GPU 側で決定した実行数を動的反映 | 不可（`drawcount` は CPU 固定値） | `vkCmdDrawIndexedIndirectCount`（`VK_KHR_draw_indirect_count`）で GPU バッファから直接読める |
| Mesh Shader ネイティブディスパッチ | 該当なし | `vkCmdDrawMeshTasksIndirectEXT` |
| フロントエンドバイパス | 事実上不可能 | ドライバ・ハードウェア世代により部分的に可能 |

`GL_ARB_indirect_parameters`（`glMultiDrawArraysIndirectCountARB` / `glMultiDrawElementsIndirectCountARB`）を利用できれば、GPU が生成した「実際のコマンド数」をそのまま Command Processor に渡せるため、無駄な `count=0` コマンドのパースを避けられる。ただし、これは OpenGL 4.3 コア仕様の範囲外であり、**クロスベンダー方針（Alloyium の非目標: NV 拡張への依存回避）とのトレードオフになる**。この拡張自体は NV/AMD/Intel いずれのベンダーでも広くサポートされているため、「Mesh Shader 拡張ほど排他的ではない」という判断のもと、将来的な採用候補として検討の余地がある（本書 §11 参照）。

---

## 6. ボトルネック分析 III — Embeddium の事前最適化とのバッティング

Mesh Shader の性能優位性が最も際立つのは、**CAD 由来の密メッシュ**（隣接三角形が多く、頂点キャッシュヒット率が低い未最適化データ）に対してである。AMD GPUOpen の Stanford Bunny 実測例（本プロジェクトの参考資料に基づく）では、未最適化状態から meshlet 分割まで進めることで変換頂点数が 143.9k → 43.2k まで削減され、重複率が 4.13× から 1.24× まで改善している。

しかし Minecraft の地形は事情が異なる：

1. **Embeddium は既に CPU 側で Greedy Meshing（貪欲メッシュ化）を行っている**。隣接する同一テクスチャの面を大きな矩形にまとめることで、そもそも生成される三角形数自体が「未最適化な密メッシュ」に比べて大幅に少ない。
2. **隣接面カリング（Face Culling）済み**。互いに接する2ブロックの内部境界面（見えない面）は、そもそもメッシュに含まれない。CAD メッシュにおける「内部の縮退三角形」に相当する無駄が、地形メッシュにはほとんど残っていない。
3. 結果として、meshlet 単位でさらに細かくカリング粒度を上げても、**削れる無駄自体が最初から少ない**ため、投資対効果（実装コスト・コマンド数増加のデメリット vs 削減できる頂点数）が CAD ケースよりも著しく低い。

したがって Alloyium は「セクション単位カリングでまず Vanilla+Embeddium 比 1.5× を確保し、meshlet 化は効果測定した上で本格投資するかを判断する」という段階的アプローチを取る。meshlet 内部の無駄が既に少ない地形メッシュでは、粒度を上げる投資対効果が CAD ケースより低いからである。

---

## 7. なぜ「正しく動くのに遅い」状態が9割地点まで見つからなかったか

これはアルゴリズムの正しさを検証するテスト（視覚的な欠け・伸びがないか、カリング後の残存数が期待通りか）だけでは検出できない種類の問題であり、プロセス上の教訓として記録する価値がある。

- **機能テストと性能テストの分離不足**: カリングの正当性（Frustum / Cone / Occlusion 別の残存数、compaction 後オフセットの重複・欠落がないこと）は早期から継続的に確認していたが、**フレーム全体の GPU time をパス別に分解する計測**は Compute B' 移行の検証に入るまで体系的に行っていなかった。「見た目が正しい」ことと「速い」ことは独立した検証軸であり、後者を後回しにするとこの種の落とし穴に気づくのが遅れる。
- **理論帯域だけを見て「小さいから問題ない」と判断するリスク**: §2.4 で示した通り、単純な帯域計算では 3% 程度にしか見えない。実際に FPS を落としていたのは同期コストとアクセスパターンの複合効果であり、**理論値の概算だけでなく実測（Nsight/RenderDoc のタイムライン）を早期から行うべきだった**。
- **「カリングが効いている」という部分的な成功が、全体の失敗を覆い隠した**: Compute A のカリング効果自体は正しく機能しており、可視頂点数は確実に減っていた。この「正しく動いている実感」が、Compute B 側の往復コストという別の変数を見落とす心理的な要因になった可能性がある。

---

## 8. 是正後アーキテクチャ（Compute B'）の詳細設計

### 8.1 パイプライン全体図

```
[Embeddium]
   セクション地形メッシュを CPU 側で生成
   → 静的 VBO / IBO（フレームを跨いで再利用、更新時のみ差分アップロード）
        │
        ▼
[Offline / Upload]
   meshlet 化（頂点再構成なし。境界球・法線コーンのみ算出）
   → MeshletHeader 配列を SSBO へアップロード
        │
        ▼
[Compute A: Amplification 相当]
   1. View-Frustum Culling（境界球 vs 6平面）
   2. Normal Cone Culling（裏面クラスタ棄却）
   3.（後期）Hi-Z Occlusion Culling
   4. Stream Compaction（Atomic Counter → shared Prefix Sum）
   → VisibleMeshletIds バッファ
   → DispatchIndirect 引数バッファ
        │
        ▼
[Compute B': Indirect コマンド生成のみ]
   可視 meshlet ごとに DrawElementsIndirectCommand を1個生成
   firstIndex / baseVertex は Embeddium 静的 IBO/VBO のオフセットをそのまま転記
   頂点属性・インデックス実データのコピーは一切行わない
   → DrawElementsIndirectCommand[] バッファ
        │
        ▼
[glMultiDrawElementsIndirect]
   Embeddium の静的 VBO/IBO を baseVertex/firstIndex で直接参照
   通常の VS+FS（またはシェーダーパックの同一 program）で描画
```

### 8.2 Compute B' が「してはいけないこと」の明文化

是正ルールをコードレベルの制約として示す：

```glsl
// ❌ 禁止パターン（旧 Compute B）
layout(std430, binding = 3) buffer OutVertices {
    PackedVertex outVerts[];
};
void main() {
    // ...
    outVerts[writeIndex] = TransformVertex(meshlet, tid); // VRAM Write
    // → 後段 VS が再度これを VRAM Read する
}

// ✅ 許可パターン（Compute B'）
layout(std430, binding = 3) buffer OutCommands {
    DrawElementsIndirectCommand cmds[];
};
void main() {
    uint visIdx = gl_GlobalInvocationID.x;
    if (visIdx >= visibleCount) return;

    uint meshletId = visibleMeshletIds[visIdx];
    MeshletHeader m = meshlets[meshletId];

    cmds[visIdx].count         = m.primitiveCount * 3;
    cmds[visIdx].instanceCount = 1u;
    cmds[visIdx].firstIndex    = m.primitiveOffset;   // Embeddium IBO オフセットの転記のみ
    cmds[visIdx].baseVertex    = int(m.vertexOffset); // Embeddium VBO オフセットの転記のみ
    cmds[visIdx].baseInstance  = 0u;
    // 頂点属性そのものには一切触れない
}
```

この対比が示す通り、Compute B' が扱うのは**インデックス値（オフセットの参照）だけ**であり、実データ（position/normal/UV 等）を読み込んでも書き込んでもいない。これにより §4 で述べた往復コストの構造そのものが除去される。

### 8.3 例外規定（頂点属性を Compute で扱ってよい唯一のケース）

将来的に遠景 LOD（メッシュ簡略化）を実装する場合、Compute で頂点を間引く・再構成する必要が生じる可能性がある。この場合に限り、以下の3条件をすべて満たすことをゲート条件とする：

1. VS では実現不可能な計算であること（例：GPU 側でのみ確定するプロシージャルな簡略化）。
2. 出力データ量が入力よりも計測上明確に小さいこと。
3. その削減量が VRAM Write+Read の往復コストを上回ることを RenderDoc/Nsight で実測していること。

Minecraft のブロック地形（軽量 VS・既に間引き済みのメッシュ）は現時点でこの3条件のいずれも満たさないため、当面は対象外とする。

---

## 9. 測定方法論：何を、どのツールで、どう測るか

Compute B' 移行の効果を「感覚」ではなく数値で確認するための具体的な手順。

### 9.1 使用ツール

| ツール | 用途 |
|---|---|
| RenderDoc | フレームキャプチャ、パス別 GPU タイムライン、SSBO の内容検査 |
| NVIDIA Nsight Graphics | パス別 GPU time、メモリ帯域使用率、Occupancy 解析（NVIDIA GPU 限定） |
| AMD Radeon GPU Profiler (RGP) | 同上（AMD GPU） |
| apitrace | GL コール列のキャプチャ・再生による回帰比較 |

### 9.2 計測すべき指標（Before/After 比較必須）

1. **パス別 GPU time**: Cull（Compute A）/ Command 生成（Compute B'）/ Draw の3区分で、フレームごとの実行時間をマイクロ秒単位で記録する。
2. **SSBO 書き込みバイト数**: Compute A・Compute B' それぞれについて、1フレームあたりの Write バイト数を RenderDoc のリソースインスペクタで確認する。Compute B' 移行後は §4.2 の見積り通り、旧実装比で大幅減少するはずである。これが確認できなければ、コード上どこかで頂点属性の書き出しが残っている可能性が高い（§11 のチェックリスト参照）。
3. **カリング後残存 meshlet（またはセクション）数**: Frustum / Cone / Occlusion 別に分解して記録する。これはロジックの正しさの検証であり、性能検証とは独立して継続する。
4. **1% low FPS**: 平均 FPS だけでなく、フレーム時間のばらつき（Command Processor のシリアライズが起きるとここに現れやすい）を確認する。
5. **`glMemoryBarrier` 前後のストール時間**: Nsight のタイムラインで、バリア命令の前後に実行ユニットのアイドル区間がないか確認する。

### 9.3 判定基準

| 指標 | 是正前（想定） | 是正後の合格ライン |
|---|---|---|
| Compute B(') の SSBO Write バイト数 | 頂点数に比例（数十〜百MB/フレーム級） | コマンド数 × 20 byte 程度（数百KB/フレーム級） |
| フレーム GPU time | Vanilla+Embeddium を上回る（悪化） | Vanilla+Embeddium を下回る |
| カリング後残存数 | 是正前後で不変（ロジックは変えていない） | 不変であることを確認（回帰チェック） |

---

## 10. それでも埋まらない差：Mesh Shader の物理的優位性の内訳

Compute B' への是正によって「往復コストで損をする」問題は解消されるが、これは Nvidium（Mesh Shader ネイティブ経路）との性能差がゼロになることを意味しない。埋まらない差の内訳を整理する。

| 要因 | Mesh Shader | Alloyium (Compute B') | 差が残る理由 |
|---|---|---|---|
| 頂点データの転送経路 | オンチップ Shader Export | 通常の頂点属性フェッチ（VRAM 経由、ただし旧Compute B比では最小） | Compute B' は往復を消したが、最終描画自体は依然として通常の GL 頂点フェッチパイプラインであり、Mesh Shader のオンチップ転送そのものより速くはならない |
| コマンド発行のオーバーヘッド | `DispatchMesh` によりコマンドプロセッサへの負荷が最小化される設計 | `glMultiDrawElementsIndirect` による逐次パース（§5） | GL 4.3 コアには GPU 側で決定した動的コマンド数をそのまま使う標準機構がない |
| Occupancy 管理 | `SetMeshOutputCounts` によるハードウェア自動制御 | ドライバ・OS スケジューラ任せ | Compute には同等の専用ハードウェア機構が存在しない |
| Task→Mesh のパイプラインオーバーラップ | ハードウェアレベルでのステージ間オーバーラップ | Compute A → Compute B' 間で `glMemoryBarrier` による明示同期が必要 | GPU 世代・ドライバにより非同期実行の余地はあるが、保証された機構ではない |

これらの差は、「アルゴリズムの移植」では埋められない**ハードウェア結合部分の差**である。Shader Export、Occupancy 自動管理、Task→Mesh のハードウェアレベル重ね合わせは、当初から再現対象外として切り分けていた。Alloyium の性能目標が「Nvidium と同等」ではなく「Vanilla+Embeddium 比で最低 1.5×、努力目標 2〜3×」に置かれているのは、この差を最初から織り込んだ設計判断である。

---

## 11. 決定事項と今後のロードマップ

### 11.1 確定した設計方針

1. Compute Shader で頂点属性（position/normal/UV）を生成・書き出しする経路は、§8.3 の例外3条件を満たさない限り実装しない。
2. 最終描画は常に Embeddium の静的 VBO/IBO を `baseVertex`/`firstIndex` で直接参照する。
3. meshlet 化によるカリング粒度向上は、セクション単位カリングのみでの到達値を計測した上で投資判断を行う（Embeddium の事前最適化により上積みが限定的である可能性が高いため）。
4. コードレビュー時に「頂点属性を書き出しているバッファが存在しないこと」を確認するチェック項目を常設する。

### 11.2 検討中の追加施策（未確定）

- `GL_ARB_indirect_parameters`（`glMultiDrawElementsIndirectCountARB`）の採用可否検討。GPU 側で決定した実コマンド数をそのまま Command Processor に渡せるため、§5 のフロントエンド負荷を軽減できる可能性がある。NV 専用拡張ではなく主要ベンダー共通の拡張であるため、クロスベンダー方針との親和性は比較的高いが、最低要求 GL バージョンの引き上げを伴うため、対応 GPU 範囲への影響を精査する必要がある。
- Hi-Z 2-pass Occlusion Culling。密なシーンでの追加利得が見込まれるが、Hi-Z 生成自体のコストがボトルネックにならないよう、ミップ生成のディスパッチ設計を検証する。
- Oculus（Iris）互換レイヤー。「シェーダーパック ON でコア機能を丸ごと無効化」を最終形にせず、Iris が要求する pass/program 契約に Alloyium の Indirect Draw を翻訳して差し込む方式を第一候補とする。

### 11.3 コミュニティへの説明方針

当初「Nvidium 相当の性能をクロスベンダーで」という期待を持たれていた経緯があるため、以下を明示する：

- Compute Shader によるエミュレーションでは、Mesh Shader のハードウェア結合部分（Shader Export、Occupancy 自動管理）は原理的に再現できない。
- 現実的な目標値は Vanilla+Embeddium 比 1.5〜3× であり、Nvidium（Mesh Shader 経路）そのものを上回ることは目指さない。
- この制約は実装上の未熟さではなく、OpenGL 4.3 の API 表面が持つ構造的な限界に起因する。

### 11.4 検証チェックリスト

- [ ] Frustum / Cone / Occlusion 別の残存 meshlet（またはセクション）数が期待どおり減る
- [ ] Compaction 後オフセットに重複・欠落がない
- [ ] Compute パス全体で頂点属性（position/normal/UV）を書き出しているバッファが存在しない（§8.3 の例外に該当しない限り）
- [ ] RenderDoc / Nsight 等で SSBO Write バイト数が旧 Compute B 比で大幅減少し、コマンド数 × 20 byte 程度に収まっている
- [ ] フレーム GPU time が Vanilla+Embeddium を下回る
- [ ] カリング後残存数が是正前後で不変（ロジック回帰なし）
- [ ] Mode A で視覚回帰がない（穴・伸び・Z-fight）
- [ ] Hi-Z 導入後、ミップ生成自体がボトルネックになっていない

---

## 12. 付録：GLSL 疑似コード集

### 12.1 Compute A（Frustum + Normal Cone Culling + Stream Compaction）

```glsl
#version 430
layout(local_size_x = 64) in;

struct MeshletHeader {
    uint  vertexOffset;
    uint  vertexCount;
    uint  primitiveOffset;
    uint  primitiveCount;
    vec4  boundingSphere; // xyz = center, w = radius
    vec4  normalCone;     // xyz = axis, w = cos(halfAngle)
};

layout(std430, binding = 0) readonly buffer Meshlets {
    MeshletHeader meshlets[];
};

layout(std430, binding = 1) buffer VisibleIds {
    uint visibleMeshletIds[];
};

layout(binding = 0, offset = 0) uniform atomic_uint visibleCount;

uniform vec4 frustumPlanes[6];
uniform vec3 cameraPos;

bool frustumCull(vec4 sphere) {
    for (int i = 0; i < 6; ++i) {
        if (dot(frustumPlanes[i].xyz, sphere.xyz) + frustumPlanes[i].w < -sphere.w) {
            return true; // culled
        }
    }
    return false;
}

bool coneCull(vec4 cone, vec3 sphereCenter) {
    vec3 viewDir = normalize(sphereCenter - cameraPos);
    return dot(cone.xyz, viewDir) > cone.w; // 完全に裏向きなら true
}

void main() {
    uint id = gl_GlobalInvocationID.x;
    if (id >= meshlets.length()) return;

    MeshletHeader m = meshlets[id];

    if (frustumCull(m.boundingSphere)) return;
    if (coneCull(m.normalCone, m.boundingSphere.xyz)) return;
    // Occlusion Culling は後期実装（Hi-Z pass 2 をここに追加）

    uint idx = atomicCounterIncrement(visibleCount);
    visibleMeshletIds[idx] = id;
}
```

### 12.2 Compute B'（Indirect コマンド生成のみ）

```glsl
#version 430
layout(local_size_x = 64) in;

struct DrawElementsIndirectCommand {
    uint count;
    uint instanceCount;
    uint firstIndex;
    int  baseVertex;
    uint baseInstance;
};

layout(std430, binding = 1) readonly buffer VisibleIds {
    uint visibleMeshletIds[];
};

layout(std430, binding = 0) readonly buffer Meshlets {
    MeshletHeader meshlets[]; // §12.1 と同一定義
};

layout(std430, binding = 2) writeonly buffer OutCommands {
    DrawElementsIndirectCommand cmds[];
};

layout(binding = 1) uniform atomic_uint visibleCountReadback;

void main() {
    uint visIdx = gl_GlobalInvocationID.x;
    if (visIdx >= atomicCounter(visibleCountReadback)) return;

    uint meshletId = visibleMeshletIds[visIdx];
    MeshletHeader m = meshlets[meshletId];

    // 頂点属性・実インデックスデータには一切アクセスしない。
    // オフセット（参照）の転記のみを行う。
    cmds[visIdx].count         = m.primitiveCount * 3u;
    cmds[visIdx].instanceCount = 1u;
    cmds[visIdx].firstIndex    = m.primitiveOffset;
    cmds[visIdx].baseVertex    = int(m.vertexOffset);
    cmds[visIdx].baseInstance  = 0u;
}
```

### 12.3 shared Prefix Sum（Wave Intrinsics 非依存の Stream Compaction 高度化版）

```glsl
#version 430
layout(local_size_x = 64) in;

shared uint localFlags[64];
shared uint groupBase;

void main() {
    uint tid = gl_LocalInvocationID.x;
    uint gid = gl_GlobalInvocationID.x;

    bool visible = /* frustum/cone/occlusion 判定結果 */ true;
    localFlags[tid] = visible ? 1u : 0u;
    barrier();

    // Hillis-Steele 型 inclusive scan（log2(64) = 6 段）
    for (uint offset = 1u; offset < 64u; offset <<= 1u) {
        uint val = (tid >= offset) ? localFlags[tid - offset] : 0u;
        barrier();
        localFlags[tid] += val;
        barrier();
    }

    if (tid == 63u) {
        groupBase = atomicCounterAdd(visibleCount, localFlags[63u]);
    }
    barrier();

    if (visible) {
        uint exclusiveScan = localFlags[tid] - 1u; // inclusive → exclusive 変換
        uint globalIdx = groupBase + exclusiveScan;
        visibleMeshletIds[globalIdx] = gid;
    }
}
```

この方式では、グローバル Atomic へのコンテンションが「スレッド数分」から「ワークグループ数分（64分の1）」に減り、単純な Atomic Counter 方式よりコンテンションを削減できる。

---

## 13. 参考文献

### Mesh Shader ハードウェア一次資料
- NVIDIA: [Introduction to Turing Mesh Shaders](https://developer.nvidia.com/blog/introduction-turing-mesh-shaders/)
- NVIDIA: [Advanced API Performance: Mesh Shaders](https://developer.nvidia.com/blog/advanced-api-performance-mesh-shaders/)
- NVIDIA: [Using Mesh Shaders for Professional Graphics](https://developer.nvidia.com/blog/using-mesh-shaders-for-professional-graphics/)
- AMD GPUOpen: [Mesh Shaders – Optimization and best practices](https://gpuopen.com/learn/mesh_shaders/mesh_shaders-optimization-and-best-practices/)
- Vulkanised 2023: Mesh shading best practices (PDF)

### Compute 移植・Stream Compaction
- zeux.io: [Meshlet size tradeoffs](https://zeux.io/2023/01/16/meshlet-size-tradeoffs/)
- zeux/meshoptimizer: https://github.com/zeux/meshoptimizer
- Raph Levien: Prefix sum on portable compute shaders
- Wicked Engine: Stream compaction vs flat bit arrays
- Khronos: Compute Shader - OpenGL Wiki

### Indirect Draw / Command Processor
- Khronos: `GL_ARB_indirect_parameters` 拡張仕様
- Khronos: `glMultiDrawElementsIndirect` リファレンス

### プロジェクト内部資料
- `DESIGN.md`（Alloyium 設計正本 v1.1）
- `MeshShader最適化のComputeShader移植.md`（アルゴリズム対応表）
- Nvidium: https://github.com/MCRcortex/nvidium
- Embeddium（本プロジェクトの地形メッシュ供給元）

---

## 総括

Alloyium の実装9割地点で発生した性能劣化は、アルゴリズムの誤りではなく、**「どこまでを Compute に移植すべきか」という設計上の境界線の引き方の誤り**に起因していた。Mesh Shader の最適化知見のうち、Amplification 段（カリング・LOD・コマンド生成）は Compute への移植適合性が高い一方、Mesh 段（頂点変換・出力）はハードウェア固有のオンチップ経路に強く依存しており、これを汎用 Compute + SSBO で模倣すると、Mesh Shader が本来負っていないコストを新たに背負い込む。

この教訓は Alloyium 固有の話にとどまらない。**「アルゴリズムとして等価に見える処理」でも、その処理が前提としているメモリ階層・実行モデルが異なれば、素朴な移植は性能面で逆効果になりうる**という、GPU プログラミング全般に通じる原則である。今後 LOD やソフトラスタライズなど、頂点データそのものを Compute で扱う可能性がある機能を検討する際は、本書 §8.3 の3条件のようなゲートを常に置き、「動くこと」と「速いこと」を独立した検証軸として扱う体制を維持する。
